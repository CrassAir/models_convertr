# Исправленные зависимости для keras-ocr с Python 3.10.10
# Решение конфликтов tensorflow-intel 2.12.1

# Основные ML библиотеки (совместимые версии)
tensorflow==2.12.1
keras>=2.12.0,<2.13.0
tensorboard>=2.12.0,<2.13.0
tensorflow-estimator>=2.12.0,<2.13.0

# Protobuf - исправляем конфликт версий
protobuf>=3.20.3,<5.0.0,!=4.21.0,!=4.21.1,!=4.21.2,!=4.21.3,!=4.21.4,!=4.21.5

# Численные вычисления
numpy>=1.21.0,<1.25.0
scipy>=1.7.0,<1.12.0

# Компьютерное зрение и обработка изображений
opencv-python>=4.5.0
pillow>=8.0.0
imageio>=2.9.0

# Аугментация изображений
imgaug>=0.4.0
albumentations>=1.1.0

# Визуализация
matplotlib>=3.5.0
seaborn>=0.11.0

# Обработка данных
pandas>=1.3.0
scikit-learn>=1.0.0

# Геометрические операции для OCR
shapely>=1.7.0
pyclipper>=1.2.0
Polygon3>=3.0.8

# Утилиты
tqdm>=4.62.0
h5py>=3.6.0

# Альтернативная установка keras-ocr для Python 3.10
# Если стандартная keras-ocr не работает:
# pip install git+https://github.com/faustomorales/keras-ocr.git

# Для GPU поддержки (опционально, только если нужен GPU):
# tensorflow-gpu==2.12.1

# Дополнительные библиотеки для работы с текстом
# python-Levenshtein>=0.12.0  # Для вычисления расстояния редактирования
